We need to improve the company candidate view.

Current situation:
- Workers can upload CVs.
- Workers can add certifications.
- Certifications are saved in public.app_worker_certifications.
- CV data is saved in public.app_14da0f1941_profiles with fields:
  cv_file_url
  cv_file_name
  cv_file_path
  cv_visible
  cv_uploaded_at

Required changes:

1. In the worker profile page:
   - Add a toggle/switch near the CV section:
     "Show CV to companies" / "Hide CV from companies"
   - This toggle must update public.app_14da0f1941_profiles.cv_visible.
   - Default value should be true if null.
   - Use try/catch/finally.
   - Show toast success/error messages.
   - Do not delete the CV when hidden.
   - Only change visibility.

2. In the company candidate detail page:
   - Fetch and display worker certifications from:
     public.app_worker_certifications
   - Filter by the selected candidate user_id.
   - Show:
     certification_name
     issuing_organization
     issue_date
     expiration_date
     credential_id
     verification_url
     certificate_file_url if available

3. In the company candidate detail page CV section:
   - Show the CV only if cv_visible = true and cv_file_url or cv_file_path exists.
   - If cv_visible = false, display:
     "This candidate has chosen not to share their CV."
   - If no CV exists, display:
     "No CV uploaded yet."

4. For private Supabase Storage:
   - If cv_file_path is available, generate a signed URL from the worker-documents bucket.
   - Use createSignedUrl(path, 3600).
   - Do the same for certificate_file_url/certificate file path if needed.

5. Add a clean UI section in the candidate detail page:
   - "Certifications"
   - Empty state: "No certifications added yet."
   - Badge for expired certifications.
   - Badge for valid certifications.
   - If expiration_date is before today, mark as "Expired".
   - If expiration_date is null, mark as "No expiry".

6. Make sure company users can read candidate certifications and CV visibility, but workers can only manage their own data.

7. Fix loading states so the candidate detail page never freezes if certifications or CV fail to load.

Important:
- Do not recreate existing tables.
- Do not remove existing profile functionality.
- Keep the current design style consistent with the existing dashboard.